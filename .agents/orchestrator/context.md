# Context

## Project Details
- **Project Name:** K-Aqua Corporate Website Implementation
- **Source Prototype:** React-SPA in `prototype/`
- **Target Tech Stack:** Next.js 15, React 19, Tailwind CSS 4, next-intl, Framer Motion
- **Current Target:** Step 21 (Performance Optimization)

## Global Constraints
- Pure languages (de, en, ar).
- RTL logic using logical CSS properties.
- Strictly semantic Tailwind tokens (no hex values).
- No inline images in code (use `<MediaSlot>`).
- Strict i18n via `next-intl` (no JSX text literals, enforced by ESLint).
