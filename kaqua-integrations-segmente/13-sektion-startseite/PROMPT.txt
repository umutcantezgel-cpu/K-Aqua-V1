SEGMENT 13 VON 27 — Vertiefung Startseite: Werk-Kennzahlen und Zertifikate

Dieser Prompt ist in sich geschlossen: Zusammen mit den Dateien in diesem Segment-Ordner enthält er alles, was zur Umsetzung nötig ist.

AUFGABE

Die Startseite um einen Abschnitt mit Werk-Kennzahlen (Kennzahlen-Band), Zertifikats-Schlagworten und einem Verweis zum Trust Center ergänzen.

KONTEXT ZUM ZIELPROJEKT (gilt vollständig, auch wenn dieses Segment einzeln ausgeführt wird)

Das Zielprojekt ist das GitHub-Repository umutcantezgel-cpu/K-Aqua-V1, Hauptzweig main. Es enthält die fertige, bereits live geschaltete Firmen-Website des Rohrsystem-Herstellers K-Aqua (erreichbar unter k-aqua-v1.vercel.app) in drei Sprachen: Deutsch, Englisch und Arabisch. Arabisch läuft von rechts nach links. Die Website ist produktiv und funktioniert — deshalb wird ausschließlich hinzugefügt; bestehender Code wird niemals umgebaut, umbenannt, gelöscht oder eigenmächtig verbessert.

So ist das Zielprojekt aufgebaut (alles ist vorhanden, nichts davon neu einrichten):
- Next.js mit App-Router. Die Seiten liegen unter app/[locale]/ mit deutschen Routennamen, zum Beispiel produkte, loesungen, unternehmen, trust-center.
- TypeScript im strikten Modus.
- Tailwind CSS in Version 4. Farben, Abstände und Schriftgrößen kommen ausschließlich aus den im Projekt definierten semantischen Token-Klassen. Es dürfen keine Hex-Farbwerte und keine frei erfundenen Größen neu in den Code geschrieben werden.
- Mehrsprachigkeit über die Bibliothek next-intl: pro Sprache genau eine Datei im Ordner messages (mindestens de.json, en.json, ar.json; eventuell weitere Sprachdateien mit englischem Platzhalterinhalt). Sichtbarer Text steht nie direkt im Komponenten-Code, sondern immer in diesen Sprachdateien. Ein Prüfskript des Projekts erzwingt, dass alle Sprachdateien exakt dieselben Schlüssel enthalten.
- Wiederverwendbare Bausteine liegen unter components/ui (unter anderem Card, Button, Reveal, SectionHead und die Icon-Sammlung icon.tsx), Seitenabschnitte unter components/sections, Werkzeuge unter components/tools, Datenmodule unter lib/data.
- In der package.json sind Prüfskripte für Produktions-Build, Lint, Typprüfung und Sprachdatei-Parität hinterlegt.

Alle beiliegenden Code-Dateien dieses Ordners sind fertig geschrieben, aufeinander abgestimmt und geprüft. Sie werden unverändert kopiert — nicht neu geschrieben, nicht umformatiert, nicht umbenannt und nicht verbessert.

VORAUSSETZUNGEN

- Segment 01 (Vorbereitung) ist abgeschlossen: Der Arbeitszweig existiert, die Prüfläufe waren zu Beginn fehlerfrei, und die Datei docs/INTEGRATIONS-PROTOKOLL.md ist angelegt.
- Segment 09 (Kennzahlen-Band) ist eingebaut — erkennbar daran, dass components/ui/StatBand.tsx existiert.

BEILIEGENDE DATEIEN UND ZIELORTE

- code/HomeDeep.tsx → components/sections/HomeDeep.tsx (neue Datei; unverändert kopieren)
- uebersetzungen/de.json, uebersetzungen/en.json, uebersetzungen/ar.json → je ein neuer Eintrag namens homedeep für die bestehenden Sprachdateien im Ordner messages (Vorgehen siehe Schritt 2)

VORGEHEN

1. Komponente ablegen: Kopiere die beiliegende Datei code/HomeDeep.tsx unverändert in das Zielprojekt an den Zielort components/sections/HomeDeep.tsx.
2. Übersetzungen einpflegen: Im Unterordner uebersetzungen liegen drei Dateien (de.json, en.json, ar.json). Jede enthält genau einen neuen obersten Eintrag namens homedeep — dieselbe Struktur in drei Sprachen; die Schlüsselgleichheit ist vorab geprüft. Füge diesen Eintrag als zusätzlichen obersten Eintrag in die jeweils passende bestehende Sprachdatei des Zielprojekts ein (messages/de.json, messages/en.json, messages/ar.json). Bestehende Einträge dürfen dabei weder verändert noch entfernt werden. Liegen im Ordner messages weitere Sprachdateien (die Liste wurde in Segment 01 notiert), erhält jede von ihnen denselben Eintrag mit dem englischen Inhalt — so bleibt die vom Prüfskript erzwungene Schlüsselgleichheit aller Sprachdateien gewahrt; genau so verfährt das Projekt bereits bei allen übrigen Einträgen.
3. Seite erweitern: Öffne im Zielprojekt die Datei app/[locale]/page.tsx. Ergänze am Dateianfang bei den vorhandenen Import-Zeilen einen zusätzlichen benannten Import für HomeDeep aus components/sections/HomeDeep — gleiche Schreibweise und gleiches Alias-Präfix wie bei den übrigen Import-Zeilen der Datei (die Komponente wird unter ihrem Namen exportiert). Füge dann das Element HomeDeep hinter dem letzten bestehenden Inhaltsabschnitt und — falls die Seite mit einem abschließenden Handlungsaufruf-Abschnitt endet — unmittelbar vor diesem ein. An dieser Datei sonst nichts ändern.
4. Führe die Prüfskripte des Projekts aus (Produktions-Build, Lint, Typprüfung; sobald Sprachdateien berührt wurden, auch die Sprachdatei-Paritätsprüfung). Alles muss fehlerfrei durchlaufen.
5. Ergänze einen kurzen Eintrag in docs/INTEGRATIONS-PROTOKOLL.md im Zielprojekt (Segmentnummer, Datum, Prüfergebnis, Besonderheiten) und hake dieses Segment in der CHECKLISTE.txt des Paket-Ordners ab.

BESONDERE HINWEISE

- Der Verweis zum Trust Center nutzt die sprachbewusste Verweis-Komponente des Projekts und ist im Code bereits fertig verdrahtet — nichts anpassen.

VERBINDLICHE REGELN

- Auf dem für diese Integration angelegten Arbeitszweig arbeiten, niemals direkt auf dem Hauptzweig. Existiert noch kein solcher Zweig, zuerst Segment 01 ausführen.
- Nur die in diesem Prompt genannten Dateien anlegen beziehungsweise exakt die genannten kleinen Ergänzungen in bestehenden Dateien machen. Keine weiteren Dateien anfassen, nichts umsortieren, nichts umformatieren.
- Keine Inhalte erfinden, keine Texte umformulieren, keine Zahlen anpassen. Fachdaten (Artikelnummern, Maße, Gewichte, Schweißwerte) sind reale Herstellerdaten und unantastbar.
- Sichtbarer Text gehört ausschließlich in die Sprachdateien im Ordner messages, niemals direkt in den Code.
- Bei Unklarheiten oder Widersprüchen zum vorgefundenen Zustand: anhalten und nachfragen statt raten, improvisieren oder den Umfang erweitern.
- In der Seitendatei nur die eine Import-Zeile und das eine Element ergänzen — keine weiteren Umbauten.
- In den Sprachdateien nur den einen neuen Eintrag ergänzen — bestehende Einträge unangetastet lassen.

PRÜFUNG (ABNAHME)

Das Segment gilt erst als abgeschlossen, wenn alle folgenden Punkte erfüllt sind:
- Alle Prüfskripte des Projekts laufen fehlerfrei durch.
- Die Startseite zeigt den neuen Abschnitt an der beschriebenen Position: Kennzahlen-Band, darunter Zertifikats-Schlagworte mit erläuternder Fußnote, darunter ein Verweis, der in jeder Sprache zur Trust-Center-Seite der jeweiligen Sprache führt.
- Stichprobe in allen drei Sprachen (Deutsch, Englisch, Arabisch): keine Mischsprache, keine Konsolen-Fehlermeldung über fehlende Übersetzungsschlüssel.
- Arabische Version: Das Layout spiegelt sauber von rechts nach links; nichts ragt aus dem Bild.
- Schmales Fenster (etwa 390 Pixel breit): Der neue Abschnitt bleibt benutzbar; Tabellen sind seitlich scrollbar.

RÜCKBAU, FALLS ETWAS SCHIEFGEHT

Die neue Komponenten-Datei löschen, die eine Import-Zeile und das eine Element aus der Seitendatei entfernen, den einen neuen Eintrag aus allen Sprachdateien entfernen. Danach die Prüfskripte erneut ausführen — sie müssen wieder fehlerfrei sein, weil nichts Bestehendes verändert wurde.
