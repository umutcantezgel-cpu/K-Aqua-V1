Runde Icon-Schaltfläche (44×44px) für Header- und Werkzeugleisten-Aktionen.

```jsx
<IconButton label="Dunkles Design aktivieren" onClick={toggle}><Moon size={19} /></IconButton>
```

Hover färbt Border + Icon in `--primary` auf `--primary-soft`-Fläche; `label` ist Pflicht (reines Icon ohne Text).
