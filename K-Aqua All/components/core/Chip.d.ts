/** Glas-Pill für Meta-Infos (ISO-Badge, Zertifikate). Nicht interaktiv. */
export interface ChipProps {
  children: React.ReactNode;
  /** optionales 14–16px Icon */
  icon?: React.ReactNode;
  style?: React.CSSProperties;
}
