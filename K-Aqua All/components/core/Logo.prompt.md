Markenzeichen: Tropfen in `--primary` mit heller Binnenlinie + Wortmarke „K-AQUA" (Outfit 800, -0.02em).

```jsx
<Logo height={28} />   /* Header */
<Logo height={26} />   /* Footer (erbt helle Farbe der dunklen Fläche) */
```

Farben laufen über Tokens — auf dunklen Flächen hellt sich die Marke automatisch auf. Nicht verzerren, nicht einfärben, keine Schatten.
